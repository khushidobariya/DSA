#include <stdio.h>
#include <conio.h>
void main(){
int arr1[10],i,length;
for(i=0;i<=9;i++){
    printf("Enter element %d: ",i+1);
    scanf("%d",&arr1[i]);
}
length=sizeof(arr1)/sizeof(arr1[0]);
printf("There are total %d elements in the array.",length);
}