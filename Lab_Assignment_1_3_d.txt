#include <stdio.h>
#include <conio.h>
void main(){
    clrscr();
    int arr1[5],i,largest;
    for(i=0;i<=4;i++){
        printf("Enter element %d: ",i+1);
        scanf("%d",&arr1[i]);
    }
    largest=arr1[0];
    for(i=1;i<=4;i++){
        if(arr1[i]>largest)
            largest=arr1[i];
    }
printf("The largest element of the array is %d.",largest);
}
