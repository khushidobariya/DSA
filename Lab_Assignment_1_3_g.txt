#include <stdio.h>
#include <conio.h>
void main(){
    clrscr();
    int arr1[50],i,num,pos;
    printf("Enter the number of elements(<50):");
    scanf("%d",&num);
    for(i=0;i<num;i++){
        printf("Enter element %d: ",i+1);
        scanf("%d",&arr1[i]);
    }
    printf("Enter the position of the element which you want to delete:");
    scanf("%d",&pos);
    for(i=pos-1;i<num-1;i++)
        arr1[i]=arr1[i+1];
    arr1[i+1]=0;
    for(i=0;i<num-1;i++)
        printf("%d\n",arr1[i]);
}
    